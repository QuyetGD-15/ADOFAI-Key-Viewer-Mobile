# Implementation plan

1. Inspect the existing three preset definitions, preset selection flow, and color configuration structure.
2. Add the `tiki` preset by following the existing preset model and wire it into the available preset list/selection behavior.
3. Add an explicit, easy-to-edit color configuration block for `tiki`, keeping its values independent from the other presets.
4. Build/analyze the affected code and verify there are no compile or inspection errors.
